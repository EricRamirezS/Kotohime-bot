/**
 * Se emite cada vez que un usuario deja de escribir en un canal.
 * @param channel El canal en el que el usuario dejó de escribir.
 * @param user El usuario que dejó de escribir.
 * @see https://discord.js.org/#/docs/main/stable/class/Channel
 * @see https://discord.js.org/#/docs/main/stable/class/User
 */
module.exports = (channel, user) => {
    //TODO
};