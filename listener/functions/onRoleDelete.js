/**
 * Se emite cada vez que se elimina un rol de SERVIDOR.
 * @param role El rol que fue eliminado
 * @see https://discord.js.org/#/docs/main/stable/class/Role
 */
module.exports = (role) => {
    //TODO
};