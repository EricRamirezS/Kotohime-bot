/**
 * Emitido cada vez que se actualiza un canal,
 * por ejemplo, cambio de nombre, cambio de tema
 * @param oldChannel El canal antes de la modificación.
 * @param newChannel El canal despues de la modificación.
 * @see https://discord.js.org/#/docs/main/stable/class/Channel
 */
module.exports = (oldChannel, newChannel) => {
    //TODO
};