/**
 * Emitido cada vez que un canal es eliminado.
 * @param channel El canal que ha sido eliminado.
 * @see https://discord.js.org/#/docs/main/stable/class/Channel
 */
module.exports = (channel) => {
    //TODO
};