/**
 * Se emite cada vez que un miembro se le revoca un veto de un servidor.
 * @param guild El gremio en el que revocó un veto.
 * @param user El usuario al que se le revocó el veto
 * @see https://discord.js.org/#/docs/main/stable/class/Guild
 * @see https://discord.js.org/#/docs/main/stable/class/User
 */
module.exports = (guild, user) => {
    //TODO
};