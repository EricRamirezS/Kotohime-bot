/**
 * Se emite cada vez que se actualiza un rol deL servidor.
 * @param oldRole El rol antes de la actualización.
 * @param newRole El rol después de la actualización
 * @see https://discord.js.org/#/docs/main/stable/class/Role
 */
module.exports = (oldRole, newRole) => {
    //TODO
};