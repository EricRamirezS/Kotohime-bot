/**
 * Se emite cada vez que se actualiza un servidor, p. Ej. cambio de nombre.
 * @param oldGuild El servidor antes de la actualización.
 * @param newGuild El servidor después de la actualización.
 * @see https://discord.js.org/#/docs/main/stable/class/Guild
 */
module.exports = (oldGuild, newGuild) => {
    //TODO
};